// @flow

type CssModuleType = {
    [string]: string;
} | {
    +[string]: string;
};

type ModsType = {
    [string]: boolean | string | number;
};

type StatesType = {
    [string]: boolean;
};

declare function Block(element?: string | null, mods?: ModsType | null, states?: StatesType | null): string;

export default (cssModule: CssModuleType, name?: string) => Block;
